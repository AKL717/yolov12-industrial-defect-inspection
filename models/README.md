# Model Files

Trained weights and exported ONNX files are not committed to this repository.

Expected local model path after training/export:

```text
models/neu_yolo12n.onnx
```

Run the training and export scripts in `scripts/` to regenerate the model locally.
